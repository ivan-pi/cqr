/* armpl_stub/armpl_interleave_batch.h
 * *** STUB — FOR DRIVER LOGIC TESTING ONLY. NOT ARM PL. ***
 * The real header declares the interleave-batch API; the stub puts
 * everything in armpl.h, so this file only needs to exist. */
#ifndef ARMPL_STUB_INTERLEAVE_BATCH_H
#define ARMPL_STUB_INTERLEAVE_BATCH_H
#include "armpl.h"
#endif
